"""
Qno.92: Use assert to check if list has at least 5 elements.

Difficult words:
- element: item in a list.
"""

items = [1, 2, 3, 4, 5]
assert len(items) >= 5, "List must have at least 5 elements"
print("List length is valid")
